# Large SAE tensors — these exceed GitHub's 100 MB per-file limit, so they
# cannot be committed. They are regenerated by scripts 01-02 (see README,
# "Data").
data/kras_sae_features.pt
data/dms_matrices/*.pt

# The repository-root .gitignore excludes results/ everywhere. This
# sub-project commits its results, so re-include them — except validation_3,
# the supplementary within-position control, which is not part of the release.
!results/
results/validation_3/

# Python
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/
